[pytest]
addopts = -ra
testpaths = tests
pythonpath = src/hosted-agent scripts
markers =
    integration: local integration tests
    external: requires external credentials or a deployed Foundry resource
    anyio: asynchronous test executed by the AnyIO pytest plugin
